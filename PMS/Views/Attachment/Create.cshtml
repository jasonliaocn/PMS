@model PMS.Models.AttachmentModels

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AttachmentModels</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.sysfilename)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.sysfilename)
            @Html.ValidationMessageFor(model => model.sysfilename)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.filename)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.filename)
            @Html.ValidationMessageFor(model => model.filename)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.mimetype)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.mimetype)
            @Html.ValidationMessageFor(model => model.mimetype)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
